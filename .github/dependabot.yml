# GENERATED FILE — every line below is written by
# .github/scripts/generate_dependabot.py. Do not edit it by hand.
#
# Nothing triggers that script for you — there is no push hook, schedule, or
# bot that keeps this in sync. Whoever adds or removes a recipe has to run it
# and commit the result in the same pull request:
#
#   python .github/scripts/generate_dependabot.py
#
# CI enforces this — the "Check dependabot.yml is up to date" job fails the
# PR if this file no longer matches the recipe tree.
#
# Ecosystems tracked: uv (Python), gomod (Go), maven (Java),
#                     gradle (Kotlin), npm (TypeScript/JS), github-actions

version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/core/python/deep-search/frontend"
    schedule:
      interval: weekly
      day: monday
      time: "02:00"
    commit-message:
      prefix: chore
      include: scope
    labels:
      - dependencies
    open-pull-requests-limit: 1
    # Wait a few days after a release before opening a PR, so the community
    # catches broken releases before we auto-merge them. Security updates
    # bypass this cooldown and fire immediately.
    cooldown:
      default-days: 7
      semver-major-days: 14
    groups:
      all-dependencies:
        patterns:
          - "*"

  - package-ecosystem: "npm"
    directory: "/core/python/genmedia-for-commerce/frontend"
    schedule:
      interval: weekly
      day: monday
      time: "02:00"
    commit-message:
      prefix: chore
      include: scope
    labels:
      - dependencies
    open-pull-requests-limit: 1
    # Wait a few days after a release before opening a PR, so the community
    # catches broken releases before we auto-merge them. Security updates
    # bypass this cooldown and fire immediately.
    cooldown:
      default-days: 7
      semver-major-days: 14
    groups:
      all-dependencies:
        patterns:
          - "*"

  - package-ecosystem: "npm"
    directory: "/core/python/long-horizon-harness/web"
    schedule:
      interval: weekly
      day: monday
      time: "02:00"
    commit-message:
      prefix: chore
      include: scope
    labels:
      - dependencies
    open-pull-requests-limit: 1
    # Wait a few days after a release before opening a PR, so the community
    # catches broken releases before we auto-merge them. Security updates
    # bypass this cooldown and fire immediately.
    cooldown:
      default-days: 7
      semver-major-days: 14
    groups:
      all-dependencies:
        patterns:
          - "*"

  - package-ecosystem: "npm"
    directory: "/core/python/long-horizon-harness/web/server"
    schedule:
      interval: weekly
      day: monday
      time: "02:00"
    commit-message:
      prefix: chore
      include: scope
    labels:
      - dependencies
    open-pull-requests-limit: 1
    # Wait a few days after a release before opening a PR, so the community
    # catches broken releases before we auto-merge them. Security updates
    # bypass this cooldown and fire immediately.
    cooldown:
      default-days: 7
      semver-major-days: 14
    groups:
      all-dependencies:
        patterns:
          - "*"

  - package-ecosystem: "uv"
    directory: "/contrib/python/market-research-agent"
    schedule:
      interval: weekly
      day: monday
      time: "02:00"
    commit-message:
      prefix: chore
      include: scope
    labels:
      - dependencies
    open-pull-requests-limit: 1
    # Wait a few days after a release before opening a PR, so the community
    # catches broken releases before we auto-merge them. Security updates
    # bypass this cooldown and fire immediately.
    cooldown:
      default-days: 7
      semver-major-days: 14
    groups:
      all-dependencies:
        patterns:
          - "*"

  - package-ecosystem: "uv"
    directory: "/core/python/ambient-expense-agent"
    schedule:
      interval: weekly
      day: monday
      time: "02:00"
    commit-message:
      prefix: chore
      include: scope
    labels:
      - dependencies
    open-pull-requests-limit: 1
    # Wait a few days after a release before opening a PR, so the community
    # catches broken releases before we auto-merge them. Security updates
    # bypass this cooldown and fire immediately.
    cooldown:
      default-days: 7
      semver-major-days: 14
    groups:
      all-dependencies:
        patterns:
          - "*"

  - package-ecosystem: "uv"
    directory: "/core/python/ambient-expense-agent/frontend"
    schedule:
      interval: weekly
      day: monday
      time: "02:00"
    commit-message:
      prefix: chore
      include: scope
    labels:
      - dependencies
    open-pull-requests-limit: 1
    # Wait a few days after a release before opening a PR, so the community
    # catches broken releases before we auto-merge them. Security updates
    # bypass this cooldown and fire immediately.
    cooldown:
      default-days: 7
      semver-major-days: 14
    groups:
      all-dependencies:
        patterns:
          - "*"

  - package-ecosystem: "uv"
    directory: "/core/python/cross-session-memory"
    schedule:
      interval: weekly
      day: monday
      time: "02:00"
    commit-message:
      prefix: chore
      include: scope
    labels:
      - dependencies
    open-pull-requests-limit: 1
    # Wait a few days after a release before opening a PR, so the community
    # catches broken releases before we auto-merge them. Security updates
    # bypass this cooldown and fire immediately.
    cooldown:
      default-days: 7
      semver-major-days: 14
    groups:
      all-dependencies:
        patterns:
          - "*"

  - package-ecosystem: "uv"
    directory: "/core/python/deep-search"
    schedule:
      interval: weekly
      day: monday
      time: "02:00"
    commit-message:
      prefix: chore
      include: scope
    labels:
      - dependencies
    open-pull-requests-limit: 1
    # Wait a few days after a release before opening a PR, so the community
    # catches broken releases before we auto-merge them. Security updates
    # bypass this cooldown and fire immediately.
    cooldown:
      default-days: 7
      semver-major-days: 14
    groups:
      all-dependencies:
        patterns:
          - "*"

  - package-ecosystem: "uv"
    directory: "/core/python/genmedia-for-commerce"
    schedule:
      interval: weekly
      day: monday
      time: "02:00"
    commit-message:
      prefix: chore
      include: scope
    labels:
      - dependencies
    open-pull-requests-limit: 1
    # Wait a few days after a release before opening a PR, so the community
    # catches broken releases before we auto-merge them. Security updates
    # bypass this cooldown and fire immediately.
    cooldown:
      default-days: 7
      semver-major-days: 14
    groups:
      all-dependencies:
        patterns:
          - "*"

  - package-ecosystem: "uv"
    directory: "/core/python/long-horizon-harness"
    schedule:
      interval: weekly
      day: monday
      time: "02:00"
    commit-message:
      prefix: chore
      include: scope
    labels:
      - dependencies
    open-pull-requests-limit: 1
    # Wait a few days after a release before opening a PR, so the community
    # catches broken releases before we auto-merge them. Security updates
    # bypass this cooldown and fire immediately.
    cooldown:
      default-days: 7
      semver-major-days: 14
    groups:
      all-dependencies:
        patterns:
          - "*"

  - package-ecosystem: "uv"
    directory: "/core/python/oauth-user-consent-flow"
    schedule:
      interval: weekly
      day: monday
      time: "02:00"
    commit-message:
      prefix: chore
      include: scope
    labels:
      - dependencies
    open-pull-requests-limit: 1
    # Wait a few days after a release before opening a PR, so the community
    # catches broken releases before we auto-merge them. Security updates
    # bypass this cooldown and fire immediately.
    cooldown:
      default-days: 7
      semver-major-days: 14
    groups:
      all-dependencies:
        patterns:
          - "*"

  - package-ecosystem: "uv"
    directory: "/core/python/rag-agent-search"
    schedule:
      interval: weekly
      day: monday
      time: "02:00"
    commit-message:
      prefix: chore
      include: scope
    labels:
      - dependencies
    open-pull-requests-limit: 1
    # Wait a few days after a release before opening a PR, so the community
    # catches broken releases before we auto-merge them. Security updates
    # bypass this cooldown and fire immediately.
    cooldown:
      default-days: 7
      semver-major-days: 14
    groups:
      all-dependencies:
        patterns:
          - "*"

  - package-ecosystem: "uv"
    directory: "/core/python/rag-vector-search"
    schedule:
      interval: weekly
      day: monday
      time: "02:00"
    commit-message:
      prefix: chore
      include: scope
    labels:
      - dependencies
    open-pull-requests-limit: 1
    # Wait a few days after a release before opening a PR, so the community
    # catches broken releases before we auto-merge them. Security updates
    # bypass this cooldown and fire immediately.
    cooldown:
      default-days: 7
      semver-major-days: 14
    groups:
      all-dependencies:
        patterns:
          - "*"

  - package-ecosystem: "uv"
    directory: "/core/python/rag-vector-search/data_ingestion"
    schedule:
      interval: weekly
      day: monday
      time: "02:00"
    commit-message:
      prefix: chore
      include: scope
    labels:
      - dependencies
    open-pull-requests-limit: 1
    # Wait a few days after a release before opening a PR, so the community
    # catches broken releases before we auto-merge them. Security updates
    # bypass this cooldown and fire immediately.
    cooldown:
      default-days: 7
      semver-major-days: 14
    groups:
      all-dependencies:
        patterns:
          - "*"

  - package-ecosystem: "uv"
    directory: "/core/python/safety-plugins"
    schedule:
      interval: weekly
      day: monday
      time: "02:00"
    commit-message:
      prefix: chore
      include: scope
    labels:
      - dependencies
    open-pull-requests-limit: 1
    # Wait a few days after a release before opening a PR, so the community
    # catches broken releases before we auto-merge them. Security updates
    # bypass this cooldown and fire immediately.
    cooldown:
      default-days: 7
      semver-major-days: 14
    groups:
      all-dependencies:
        patterns:
          - "*"

  - package-ecosystem: "uv"
    directory: "/core/rag-agent-search"
    schedule:
      interval: weekly
      day: monday
      time: "02:00"
    commit-message:
      prefix: chore
      include: scope
    labels:
      - dependencies
    open-pull-requests-limit: 1
    # Wait a few days after a release before opening a PR, so the community
    # catches broken releases before we auto-merge them. Security updates
    # bypass this cooldown and fire immediately.
    cooldown:
      default-days: 7
      semver-major-days: 14
    groups:
      all-dependencies:
        patterns:
          - "*"

  - package-ecosystem: "uv"
    directory: "/core/rag-vector-search"
    schedule:
      interval: weekly
      day: monday
      time: "02:00"
    commit-message:
      prefix: chore
      include: scope
    labels:
      - dependencies
    open-pull-requests-limit: 1
    # Wait a few days after a release before opening a PR, so the community
    # catches broken releases before we auto-merge them. Security updates
    # bypass this cooldown and fire immediately.
    cooldown:
      default-days: 7
      semver-major-days: 14
    groups:
      all-dependencies:
        patterns:
          - "*"

  - package-ecosystem: "uv"
    directory: "/core/rag-vector-search/data_ingestion"
    schedule:
      interval: weekly
      day: monday
      time: "02:00"
    commit-message:
      prefix: chore
      include: scope
    labels:
      - dependencies
    open-pull-requests-limit: 1
    # Wait a few days after a release before opening a PR, so the community
    # catches broken releases before we auto-merge them. Security updates
    # bypass this cooldown and fire immediately.
    cooldown:
      default-days: 7
      semver-major-days: 14
    groups:
      all-dependencies:
        patterns:
          - "*"

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: weekly
      day: monday
      time: "02:00"
    commit-message:
      prefix: chore
      include: scope
    labels:
      - dependencies
      - github-actions
    open-pull-requests-limit: 1
    # Wait a few days after a release before opening a PR, so the community
    # catches broken releases before we auto-merge them. Security updates
    # bypass this cooldown and fire immediately.
    cooldown:
      default-days: 7
    groups:
      all-actions:
        patterns:
          - "*"
