{
  "eval_set_id": "background_processes",
  "name": "Background Processes — terminal(background=) + process()",
  "description": "Behavioral evals for the agent's use of the background-process surface. The two tools under test (both in horizon/tools/processes/): (a) `terminal` with `background=True` spawns a long-running command into a session-scoped registry and returns `{session_id, pid, status: 'running'}` immediately; with the default `on_timeout='background'` a foreground command that exceeds `timeout_s` auto-promotes into the same registry rather than getting killed. (b) `process(action=...)` drives the lifecycle — `list`, `poll`, `log`, `wait`, `kill`, `write`. The behaviors these evals pin: (1) the model picks background= for commands it knows will outlive a single turn (dev servers, long builds, watching for an event); (2) when a command times out it follows up with `process(action='wait')` or `process(action='kill')` instead of giving up; (3) when a foreground command hangs on an interactive prompt, the model recovers via either re-running with non-interactive flags (-y, --yes) OR writing to stdin via `process(action='write')`. NOTE: project eval_config.json grades against rubric_based_final_response_quality_v1 only — tool selection is encoded in rubric text, not via a trajectory grader.",
  "eval_cases": [
    {
      "eval_id": "long_running_command_uses_background",
      "conversation": [
        {
          "user_content": {
            "parts": [{"text": "Start the dev server with `npm run dev` and keep it running while you check that it's serving on port 3000."}]
          },
          "intermediate_data": {
            "tool_uses": [
              {"name": "terminal", "args": {"command": "npm run dev", "background": true}},
              {"name": "process", "args": {"action": "poll"}}
            ]
          },
          "rubrics": [
            {
              "rubric_id": "background_true_for_dev_server",
              "rubric_content": {
                "text_property": "The response invokes `terminal` with `background=True` (NOT a default foreground call) for `npm run dev`. A dev server is the textbook background case — it runs forever, blocking the turn would never return. A response that calls `terminal` foreground and waits for it to exit fails this rubric."
              }
            },
            {
              "rubric_id": "uses_process_tool_to_inspect",
              "rubric_content": {
                "text_property": "After spawning the background session, the response inspects it via the `process` tool (e.g. `process(action='poll')` to see boot output, or `process(action='log')`) instead of just claiming the server started. Reporting success without checking is the failure mode this rubric guards against."
              }
            }
          ]
        }
      ],
      "session_input": {
        "app_name": "app",
        "user_id": "eval_shared",
        "state": {}
      }
    },
    {
      "eval_id": "timeout_followed_by_wait_not_giveup",
      "conversation": [
        {
          "user_content": {
            "parts": [{"text": "Run the full test suite with `pytest tests/` and tell me the result. It usually takes about 3 minutes."}]
          },
          "intermediate_data": {
            "tool_uses": [
              {"name": "terminal", "args": {"command": "pytest tests/"}},
              {"name": "process", "args": {"action": "wait"}}
            ]
          },
          "rubrics": [
            {
              "rubric_id": "follows_up_on_timeout_with_wait_or_poll",
              "rubric_content": {
                "text_property": "When `terminal` returns `{timed_out: true, status: 'running', session_id: ...}` (auto-promotion), the response follows up by calling `process(action='wait', session_id=...)` (or repeated `process(action='poll', ...)`) on the returned session_id rather than giving up, reporting failure, or re-running the command from scratch. The whole point of `on_timeout='background'` is that the agent can keep watching — not following up wastes the partial progress."
              }
            },
            {
              "rubric_id": "does_not_kill_unrelated_to_request",
              "rubric_content": {
                "text_property": "The response does NOT call `process(action='kill', ...)` on the test-run session unless the user asks to stop it. The user wanted the result; killing aborts the run and loses progress. Killing is appropriate for runaway commands or when the user changes their mind — neither applies here."
              }
            }
          ]
        }
      ],
      "session_input": {
        "app_name": "app",
        "user_id": "eval_shared",
        "state": {}
      }
    },
    {
      "eval_id": "interactive_recovery_via_nonints_flag",
      "conversation": [
        {
          "user_content": {
            "parts": [{"text": "Install `ripgrep` via apt."}]
          },
          "intermediate_data": {
            "tool_uses": [
              {"name": "terminal", "args": {"command": "apt-get install -y ripgrep"}}
            ]
          },
          "rubrics": [
            {
              "rubric_id": "recovers_from_interactive_prompt",
              "rubric_content": {
                "text_property": "If the agent's first attempt at installing ripgrep returns an interactive-prompt signal (the runtime detects a `[Y/n]` or `password:` wait and either auto-promotes or returns an `interactive: true` hint), the response recovers — either by re-running with `-y`/`--yes`/`--assume-yes`, or by using `process(action='write', data='y')` against the registered session. A response that gives up after the first prompt-stuck call, or re-runs the same interactive command without changing it, fails."
              }
            },
            {
              "rubric_id": "prefers_noninteractive_flag_over_stdin_write",
              "rubric_content": {
                "text_property": "When both options are available, the response prefers the non-interactive flag (`-y`) over `process(action='write')`. Re-running with `-y` is idempotent and side-effect-free; writing `y` to stdin works but is brittle if the prompt copy changes. A response that immediately reaches for `process(write)` without trying the flag is technically OK but less robust — only the worst pattern (giving up entirely) fully fails this rubric."
              }
            }
          ]
        }
      ],
      "session_input": {
        "app_name": "app",
        "user_id": "eval_shared",
        "state": {}
      }
    },
    {
      "eval_id": "short_command_does_not_use_background",
      "conversation": [
        {
          "user_content": {
            "parts": [{"text": "What's the current git branch?"}]
          },
          "intermediate_data": {
            "tool_uses": [
              {"name": "terminal", "args": {"command": "git branch --show-current"}}
            ]
          },
          "rubrics": [
            {
              "rubric_id": "no_background_for_trivial_command",
              "rubric_content": {
                "text_property": "The response calls `terminal` foreground (no `background=True`, no `process` follow-up) for `git branch --show-current`. The command exits in milliseconds — using `background=True` is overkill and forces a needless second tool call to retrieve the result. This rubric catches over-eager use of the new surface."
              }
            }
          ]
        }
      ],
      "session_input": {
        "app_name": "app",
        "user_id": "eval_shared",
        "state": {}
      }
    }
  ]
}
