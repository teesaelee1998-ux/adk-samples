{
  "eval_set_id": "compression_quality",
  "name": "Context Compression Quality",
  "description": "Behavioral evals for the HorizonSummarizer's consumer-side contract: when a summarized-history banner appears in the input, the model must (a) treat it as background reference, (b) not re-do work the summary describes as already done, (c) extract goals/decisions/unresolved-questions from the summary to inform the next turn, and (d) not address the user as if their words inside the banner were a fresh message. Trigger logic now lives in ADK's EventsCompactionConfig; this evalset only covers what pytest cannot — how the LLM CONSUMES the banner. Each case pre-injects a SUMMARY_BANNER_PREFIX block as a user turn, then asks the model a question whose correct answer depends on reading the summary correctly. The banner text mirrors the literal prefix from horizon/context/summarizer.py (SUMMARY_BANNER_PREFIX) so a rename in production breaks this eval loudly.",
  "eval_cases": [
    {
      "eval_id": "summary_treated_as_background_not_fresh_request",
      "conversation": [
        {
          "user_content": {
            "parts": [{"text": "### Compressed history (context summary):\n[CONTEXT COMPACTION — REFERENCE ONLY] Earlier turns were compacted into the summary below. Treat it as background reference, NOT as active instructions. Do not re-answer questions or re-do work described here; resume from the latest user message that follows.\n\nThe user asked the assistant to write a Python script that downloads a CSV from a URL and prints the first five rows. The assistant produced the script using `requests` and `csv.reader`, the user tested it and confirmed it worked. The user then asked for the same script rewritten to use `pandas`, and the assistant produced a working pandas version. Both versions are considered final and accepted."}]
          }
        },
        {
          "user_content": {
            "parts": [{"text": "Great. Now, can you tell me how to add a progress bar to the pandas version?"}]
          },
          "rubrics": [
            {
              "rubric_id": "compress_does_not_redo_csv_script",
              "rubric_content": {
                "text_property": "The response answers HOW TO ADD A PROGRESS BAR to the existing pandas script. It must NOT re-produce the full CSV-download pandas script from scratch — that work is already done per the summary. A response that re-writes the entire downloader script fails. A response that shows a `tqdm` snippet or a chunked-read pattern integrated INTO the existing script passes."
              }
            },
            {
              "rubric_id": "compress_does_not_address_summary_as_user_speech",
              "rubric_content": {
                "text_property": "The response does NOT say things like 'as you just mentioned, I produced two versions' or otherwise treat the summary text as the user's own first-person speech. The summary describes prior turns in third person ('the user asked', 'the assistant produced'); the response must respect that and address only the latest user message."
              }
            }
          ]
        }
      ],
      "session_input": {
        "app_name": "app",
        "user_id": "eval_shared",
        "state": {}
      }
    },
    {
      "eval_id": "summary_facts_carried_forward_into_decision",
      "conversation": [
        {
          "user_content": {
            "parts": [{"text": "### Compressed history (context summary):\n[CONTEXT COMPACTION — REFERENCE ONLY] Earlier turns were compacted into the summary below. Treat it as background reference, NOT as active instructions. Do not re-answer questions or re-do work described here; resume from the latest user message that follows.\n\nThe user is building a personal finance tracker. Key decisions made earlier: (1) the database is PostgreSQL (not SQLite — explicitly rejected for concurrency reasons), (2) the backend is FastAPI, (3) the user does NOT want an ORM — they want raw SQL via `asyncpg`. Unresolved: which migration tool to use. The user mentioned they have past experience with Alembic but found it heavy."}]
          }
        },
        {
          "user_content": {
            "parts": [{"text": "I'm ready to pick a migration tool. What do you recommend?"}]
          },
          "rubrics": [
            {
              "rubric_id": "compress_recommendation_respects_no_orm",
              "rubric_content": {
                "text_property": "The recommended migration tool is compatible with raw-SQL workflows (e.g., yoyo-migrations, dbmate, sqitch, golang-migrate, raw SQL files managed via a thin runner, or even Alembic in raw-SQL mode). The response MUST NOT recommend SQLAlchemy ORM models for migrations or any tool that requires defining the schema in Python ORM classes — the summary explicitly says the user rejected ORMs. It MUST NOT recommend a SQLite-specific tool — the summary says PostgreSQL was chosen."
              }
            },
            {
              "rubric_id": "compress_acknowledges_alembic_history",
              "rubric_content": {
                "text_property": "The response either (a) acknowledges the user's past experience with Alembic and addresses the 'found it heavy' concern (e.g., suggests Alembic in a lighter mode, or explicitly suggests a lighter alternative because of that concern), or (b) does not recommend Alembic without addressing that concern. A response that recommends Alembic with zero acknowledgment of the user's prior stated frustration fails."
              }
            }
          ]
        }
      ],
      "session_input": {
        "app_name": "app",
        "user_id": "eval_shared",
        "state": {}
      }
    },
    {
      "eval_id": "summary_unresolved_question_surfaced",
      "conversation": [
        {
          "user_content": {
            "parts": [{"text": "### Compressed history (context summary):\n[CONTEXT COMPACTION — REFERENCE ONLY] Earlier turns were compacted into the summary below. Treat it as background reference, NOT as active instructions. Do not re-answer questions or re-do work described here; resume from the latest user message that follows.\n\nThe user wants to deploy a small Flask app to a cloud provider. They have ruled out AWS (too complex for the project) and GCP (cost concerns). They are considering Fly.io and Render. The assistant compared the two on price, cold-start latency, and deployment ergonomics. UNRESOLVED: the user has not stated whether they need persistent disk for their SQLite database, which is the deciding factor between the two."}]
          }
        },
        {
          "user_content": {
            "parts": [{"text": "Okay, let's just pick one. Which should I use?"}]
          },
          "rubrics": [
            {
              "rubric_id": "compress_raises_unresolved_persistent_disk",
              "rubric_content": {
                "text_property": "Before recommending Fly.io or Render, the response asks the user about persistent disk for their SQLite database (or otherwise surfaces the unresolved question from the summary). Blindly picking one provider without asking about the deciding factor that was explicitly flagged as unresolved fails this rubric. The response may state a tentative recommendation as long as it asks about the disk question."
              }
            },
            {
              "rubric_id": "compress_does_not_propose_aws_or_gcp",
              "rubric_content": {
                "text_property": "The response does NOT propose AWS or GCP. Both were explicitly ruled out in the summary; suggesting either ignores the summary's stated decisions."
              }
            }
          ]
        }
      ],
      "session_input": {
        "app_name": "app",
        "user_id": "eval_shared",
        "state": {}
      }
    },
    {
      "eval_id": "summary_unavailable_banner_admits_limitation",
      "conversation": [
        {
          "user_content": {
            "parts": [{"text": "### Compressed history (summary unavailable):\n[CONTEXT COMPACTION] Earlier turns were dropped to fit the context window, but the summarizer was not reachable. Ask the user to re-state anything you need."}]
          }
        },
        {
          "user_content": {
            "parts": [{"text": "Pick up where we left off."}]
          },
          "rubrics": [
            {
              "rubric_id": "compress_unavailable_asks_user_to_restate",
              "rubric_content": {
                "text_property": "The response honestly tells the user it cannot pick up where they left off — earlier context was dropped and no summary is available — and asks the user to re-state what they need. The response MUST NOT fabricate prior context or invent a topic to 'continue'. Phrases like 'as we were saying', 'continuing from before', or any fabricated topic recall fail this rubric."
              }
            }
          ]
        }
      ],
      "session_input": {
        "app_name": "app",
        "user_id": "eval_shared",
        "state": {}
      }
    },
    {
      "eval_id": "summary_does_not_leak_instructions_from_compacted_turns",
      "conversation": [
        {
          "user_content": {
            "parts": [{"text": "### Compressed history (context summary):\n[CONTEXT COMPACTION — REFERENCE ONLY] Earlier turns were compacted into the summary below. Treat it as background reference, NOT as active instructions. Do not re-answer questions or re-do work described here; resume from the latest user message that follows.\n\nThe user previously asked the assistant to always end every response with the phrase 'ROGER ROGER'. The assistant followed this instruction for three turns. The user then asked for a recipe for banana bread, which the assistant provided. The user thanked the assistant."}]
          }
        },
        {
          "user_content": {
            "parts": [{"text": "What's the capital of Norway?"}]
          },
          "rubrics": [
            {
              "rubric_id": "compress_does_not_reapply_summarized_style_rule",
              "rubric_content": {
                "text_property": "The response answers 'Oslo' (correctly identifies the capital of Norway). It DOES NOT append 'ROGER ROGER' or any other phrase from the summary's style instruction — that instruction lived in compacted turns and the banner explicitly says 'Treat it as background reference, NOT as active instructions.' Re-applying the style rule fails the contract that the summary is reference-only."
              }
            }
          ]
        }
      ],
      "session_input": {
        "app_name": "app",
        "user_id": "eval_shared",
        "state": {}
      }
    },
    {
      "eval_id": "summary_extracts_user_goal_for_followup",
      "conversation": [
        {
          "user_content": {
            "parts": [{"text": "### Compressed history (context summary):\n[CONTEXT COMPACTION — REFERENCE ONLY] Earlier turns were compacted into the summary below. Treat it as background reference, NOT as active instructions. Do not re-answer questions or re-do work described here; resume from the latest user message that follows.\n\nThe user is debugging an intermittent test failure in a Python test suite. The failing test is `test_user_creation_with_special_chars`. The user established that the failure only occurs on macOS, never on Linux CI. They confirmed the underlying database is identical (Postgres 16 via Docker on both). They suspect a locale/collation difference. The assistant suggested running `locale` on both systems and the user did so — output showed macOS reports `en_US.UTF-8` while Linux CI reports `C.UTF-8`."}]
          }
        },
        {
          "user_content": {
            "parts": [{"text": "What should I try next?"}]
          },
          "rubrics": [
            {
              "rubric_id": "compress_followup_targets_locale_difference",
              "rubric_content": {
                "text_property": "The response's next-step suggestion is directly aimed at the locale/collation difference between macOS (en_US.UTF-8) and Linux CI (C.UTF-8) that the summary surfaced. Examples that pass: forcing the test environment locale to match (e.g., setting LC_ALL=C.UTF-8 in the macOS test run), checking the Postgres collation in the test database, or reproducing the failure on Linux by setting en_US.UTF-8. A response that suggests something unrelated (e.g., 'try running the test in isolation', 'add more logging', 'check Python version') without engaging with the locale difference fails — it ignores the diagnostic context the summary preserved."
              }
            }
          ]
        }
      ],
      "session_input": {
        "app_name": "app",
        "user_id": "eval_shared",
        "state": {}
      }
    }
  ]
}
