[project]
name = "cross-session-memory"
version = "0.1.0"

dependencies = [
    "google-adk>=1.15.0,<2.0.0",
    "opentelemetry-instrumentation-google-genai>=0.1.0,<1.0.0",
    "gcsfs>=2026.7.0",
    "google-cloud-logging>=3.16.1,<4.0.0",
    "google-cloud-aiplatform[evaluation,agent-engines]>=1.162.0",
    "protobuf>=6.31.1,<7.0.0",
    "python-dotenv>=1.0.0,<2.0.0",
    "fastapi>=0.140.0,<1.0.0",
    "uvicorn~=0.51.0",
]
requires-python = ">=3.11,<3.14"


[dependency-groups]
dev = [
    "google-adk[eval]>=1.15.0,<2.0.0",
    "agent-starter-pack>=0.14.1",
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
]

[project.optional-dependencies]
lint = [
    "ruff>=0.16.0,<1.0.0",
    "ty>=0.0.63",
    "codespell>=2.4.3,<3.0.0",
]

[tool.ty]
# ty is Astral's Rust-based type checker (same team as ruff/uv)
# See: https://docs.astral.sh/ty/

[tool.ty.environment]
python-version = "3.10"

[tool.ty.src]
exclude = [".venv/**"]

[tool.ty.rules]
# Ignore common issues with third-party libraries and dynamic code patterns
unresolved-import = "ignore"
unresolved-attribute = "ignore"
invalid-argument-type = "ignore"
invalid-assignment = "ignore"
invalid-return-type = "ignore"
possibly-missing-attribute = "ignore"
not-subscriptable = "ignore"
deprecated = "ignore"

[tool.codespell]
ignore-words-list = "rouge"
skip = "./locust_env/*,uv.lock,.venv,**/package-lock.json"


[build-system]
requires = ["uv_build>=0.11.32,<0.12.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-root = ""
module-name = "app"

# This configuration file is used by goo.gle/agent-starter-pack to power remote templating.
# It defines the template's properties and settings.
[tool.agent-starter-pack]
example_question = "I live in Austin. What is the weather there?"

[tool.agent-starter-pack.settings]
agent_directory = "app"

[[tool.uv.index]]
url = "https://pypi.org/simple/"
default = true
