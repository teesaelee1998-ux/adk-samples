[project]
name = "expense-approval-ui"
version = "0.1.0"
description = "IAP-protected approval UI for the ambient expense agent."
requires-python = ">=3.11,<3.13"
license = "Apache-2.0"

dependencies = [
    "fastapi>=0.140.0",
    "uvicorn>=0.51.0",
    "httpx>=0.27.0",
    "google-auth>=2.56.2",
    "requests>=2.34.2",
]
